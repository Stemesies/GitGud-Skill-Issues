import { Routes } from '@angular/router';
import { IssuesList } from './src/components/IssuesList';

export const routes: Routes = [
    { path: '', component: IssuesList }
    //{ path: 'new', component: IssuesList }
];
